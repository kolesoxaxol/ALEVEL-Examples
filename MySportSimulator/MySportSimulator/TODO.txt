1)	Сделать реализацию событий единым списком: во время загрузки матча
	  весь матч забивается в коллекцию событий, запускается таймер и по каждому тику берется одно
	  событие и выводится в логч
	  Плюсы:
	   1) разгружаем вычисления по тику таймера
	   2) легкость реализации системы "реплеев"
	  Минусы:
	   1) реализация доп. хранилища
	   2) "обман" пользователя
3) реализовать доп. факторы выпадения событий (агрессивность судья для штрафного, штрафной-гол)
4) реализовать удаление игроков и замены.